# Example environment file for the MoodJournal project
# Copy this to `.env` or `example.env` and fill in real values.

DEBUG=False

# Keep this secret (DO NOT use the example value in production)
SECRET_KEY=replace-me-with-a-secret-key

# Database settings (used to build DATABASE_URL in env.py)
DB_NAME=django_db
DB_USER=journal_rw_a4d2b9
DB_PASSWORD=replace-with-strong-password


